# K3 Max parity summary

The repository now treats exactness, benchmark-profile compatibility, measured benchmark scores, and multimodal capability as separate gates. Runtime optimizations cannot convert a missing capability or an unmeasured benchmark into a green full-parity claim.
